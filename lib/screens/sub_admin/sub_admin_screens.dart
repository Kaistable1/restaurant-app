import 'package:flutter/material.dart';

class SubAdminScreens extends StatelessWidget {
  const SubAdminScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('subAdmin')
      ],
    );
  }
}
